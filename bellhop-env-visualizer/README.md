# Bellhop .env Visualizer

Parses and visualizes Bellhop environment files.

## Usage

Open `index.html` in browser. Drop a .env file on it.

Shows:
- Sound speed profile (depth vs speed)
- Geometry (sources, receivers, boundaries)
- Parameters table

## Limitations

- First run type only (if file has multiple)
- Standard format only
